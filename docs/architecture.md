# Architecture
#
# High-level architecture documentation for OpenKiln.
# Covers: pipeline model (Source -> Transform -> Sink),
# workflow YAML format, database schema, config system,
# and provider plugin interface.
